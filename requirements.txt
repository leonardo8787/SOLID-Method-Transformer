pandas>=1.1.0
numpy>=1.19.0
scipy>=1.5.0
scikit-learn>=0.24.0
matplotlib>=3.0.0

# Opcional, mas recomendado para reprodutibilidade
python-dateutil
pytz

# Nota: se você usar um ambiente Python mais recente, versões mínimas podem ser ajustadas

# Dependências para gerar embeddings CLIP (pode exigir instalação CUDA para torch)
torch>=1.8.0
torchvision
ftfy
regex
tqdm
# OpenAI CLIP (instalação via git)
git+https://github.com/openai/CLIP.git
